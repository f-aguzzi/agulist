import { Text, View } from 'react-native';
import { styles } from '../definitions/styles';

function EntryPage() {
    return (
        <View style={styles.container}>
            <Text style={styles.title}>AguList</Text>
        </View>
    )
}

export default EntryPage;